# Repository Guidance

- Keep `music21-rs` library code application-agnostic. Functionality that exists only to render, lay out, or adapt data for `examples/web` belongs in the web example layer, not under `src/`.
