{
  "name": "music21-rs-web",
  "version": "0.0.0",
  "description": "Browser demos for music21-rs",
  "private": true,
  "license": "AGPL-3.0-only",
  "scripts": {
    "build": "wasm-pack build . --target web --out-dir pkg --release && npx --yes --package typescript@5.9.3 tsc -p tsconfig.json",
    "build:dev": "wasm-pack build . --target web --out-dir pkg --dev && npx --yes --package typescript@5.9.3 tsc -p tsconfig.json",
    "build:wasm": "wasm-pack build . --target web --out-dir pkg --release",
    "build:wasm:dev": "wasm-pack build . --target web --out-dir pkg --dev",
    "typecheck": "npx --yes --package typescript@5.9.3 tsc -p tsconfig.json"
  }
}
