# Docstrings of music21.analysis.harmonicFunction whose every example passes against the
# music21-rs facades. Written by `UPDATE_DOCTEST_EXPECTATIONS=1 cargo test
# --manifest-path python-parity/Cargo.toml --test doctest_harmonicfunction`; the test
# fails when one of these stops passing.

passing = [
    "music21.analysis.harmonicFunction.functionToRoman",
    "music21.analysis.harmonicFunction.romanToFunction",
]
