# Docstrings of music21.analysis.enharmonics whose every example passes against the
# music21-rs facades. Written by `UPDATE_DOCTEST_EXPECTATIONS=1 cargo test
# --manifest-path python-parity/Cargo.toml --test doctest_enharmonics`; the test
# fails when one of these stops passing.

passing = ["music21.analysis.enharmonics.EnharmonicSimplifier.bestPitches"]
